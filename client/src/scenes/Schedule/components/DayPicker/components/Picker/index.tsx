import * as React from 'react';

import PickerWrapper from '../PickerWrapper';
import { IPickerProps } from './types';

const Picker = ({ visible }: IPickerProps) => (
  <PickerWrapper visible={visible}>

  </PickerWrapper>
)

export default Picker;
